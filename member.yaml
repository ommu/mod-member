profile_submenu:
  view:
    label: Detail
    url:
      0: 'setting/profile/admin/view'
      id: $_GET*id
    select: action
  update:
    label: Update
    url:
      0: 'setting/profile/admin/update'
      id: $_GET*id
    select: action
  category:
    label: Categories
    url:
      0: 'setting/profile/category/manage'
      profile: $_GET*id
    select: controller
  document:
    label: Documents
    url:
      0: 'setting/profile/document/manage'
      profile: $_GET*id
    select: controller
  delete:
    label: Delete
    url:
      0: 'setting/profile/admin/delete'
      id: $_GET*id
    htmlOptions:
      data-confirm: Are you sure you want to delete this item?
      data-method: post
    select: action
setting_submenu:
  update:
    label: Settings
    url: ['setting/admin/update']
    select: controller
  category:
    label: Categories
    url: ['setting/category']
    select: controller